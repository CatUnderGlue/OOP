package part3;

public class Bus extends Transport{
    public Bus(String brand, String model, int releaseYer, String productionCountry, String bodyColor, int maxSpeed) {
        super(brand, model, releaseYer, productionCountry, bodyColor, maxSpeed);
    }
}
